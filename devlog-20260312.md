# Devlog 2026-03-12

## Home Surface Host Menu

- Clarified that `src/menu_ui.py` currently implements the mute-button settings overlay, not the primary home surface.
- Added a preventative lesson to avoid confusing the home surface with the settings overlay in future UI work.
- Wrote `docs/home-surface-host-menu-sdd.md` to define the new default screen:
  - keep Spotifone branding
  - show `Bluetooth mic + keyboard. no setup, just talk.`
  - add purple divider
  - list remembered Bluetooth hosts with bright live rows and muted offline rows
  - tap host to switch/connect
- Added Phase 5 tracking to `tasks/todo.md` for implementation and verification.
- Reworked `src/menu_ui.py` so the default home surface is now a branded host menu instead of a passive splash:
  - header copy: `Bluetooth mic + keyboard.` + `no setup, just talk.`
  - purple divider line
  - remembered host rows with bright live/connected state and muted offline state
  - home-surface tap routing into the existing host connect flow
- Kept the mute-button overlay path intact for scan/delete/about rather than deleting the existing settings surface.
- Added `tests/test_menu_ui.py` for host-state merge, discovery filtering, and home-surface tap routing.
- Verification:
  - `python3 -m unittest /Users/seanslab/seanslab/spotifone/tests/test_menu_ui.py -v`
  - `python3 -m unittest discover -s /Users/seanslab/seanslab/spotifone/tests -v`
  - Result: 138 tests passed
- Follow-up refinement requested:
  - remove MAC addresses from the home surface
  - switch the host chooser to a 2-column icon grid with iOS-home-screen energy
- Implemented the refinement in `src/menu_ui.py`:
  - 2-column host tile layout
  - simple device icon in each tile
  - centered host labels
  - no MAC addresses on the home surface
- Branding tweak:
  - render `spotif` in white and `one` in purple in both the home surface and settings header
- Follow-up refinement requested:
  - highlight the current connected host with a purple rectangle on the home grid
  - move that highlight to the new host after a successful switch
- Implemented connected-host highlighting on the home grid:
  - active host tile now uses a purple rectangle outline
  - highlight follows `connected` state after host switch/refresh
- Expanded `tests/test_menu_ui.py` with connected-tile style assertions.
- Verification refresh:
  - `python3 -m unittest /Users/seanslab/seanslab/spotifone/tests/test_menu_ui.py -v`
  - `python3 -m unittest discover -s /Users/seanslab/seanslab/spotifone/tests -v`
  - Result: 142 tests passed
- New refinement requested:
  - add a tiny vertical key legend on the right side of the home surface
  - labels: `Left`, `Right`, `Del`, `Enter`, `Menu`
- Implemented right-side home legend rail:
  - five compact labels on the right edge: `Left`, `Right`, `Del`, `Enter`, `Menu`
  - host grid width reduced to preserve the 2-column layout without overlap
- Expanded `tests/test_menu_ui.py` with legend layout assertions.
- Verification refresh:
  - `python3 -m unittest /Users/seanslab/seanslab/spotifone/tests/test_menu_ui.py -v`
  - `python3 -m unittest discover -s /Users/seanslab/seanslab/spotifone/tests -v`
  - Result: 143 tests passed
- Expanded `tests/test_menu_ui.py` to cover:
  - 2-column layout
  - right-column tap routing
- Verification refresh:
  - `python3 -m unittest /Users/seanslab/seanslab/spotifone/tests/test_menu_ui.py -v`
  - `python3 -m unittest discover -s /Users/seanslab/seanslab/spotifone/tests -v`
  - Result: 140 tests passed
- New refinement requested:
  - align the on-screen key legend with the Car Thing's physical controls
  - improve the visual treatment so the legend feels intentional and elegant
- Reworked the right-edge legend in `src/menu_ui.py`:
  - `Menu` moved up toward the hardware menu-button zone
  - `Left`, `Enter`, and `Right` clustered around the dial region
  - `Del` moved lower to match the lower-right control area
  - `Menu` and `Enter` now use a stronger purple-accent pill treatment for quicker scanning
- Expanded `tests/test_menu_ui.py` with hardware-region ordering assertions for the legend layout.
- Verification refresh:
  - `python3 -m unittest /Users/seanslab/seanslab/spotifone/tests/test_menu_ui.py -v`
  - `python3 -m unittest discover -s /Users/seanslab/seanslab/spotifone/tests -v`
  - Result: 144 tests passed
- Follow-up correction requested:
  - the right-side legend should stay vertical because the hardware buttons are perceived as a vertical strip
- Refined the right-edge legend again in `src/menu_ui.py`:
  - replaced the clustered control map with a single vertical rail
  - stacked `Left`, `Right`, `Del`, `Enter`, and `Menu` in one column
  - added a subtle rail line plus purple emphasis around `Enter` and `Menu`
- Updated `tests/test_menu_ui.py` to enforce vertical ordering and near-aligned x positions for the rail.
- Follow-up correction requested:
  - the vertical rail was still too wide and the locations still felt off relative to the hardware edge
- Tightened the right-edge legend in `src/menu_ui.py`:
  - reduced the reserved legend width so the host grid regains space
  - moved the labels closer to the physical screen edge
  - slimmed each label into a narrow tab instead of a wide pill
  - changed the purple emphasis to a thin edge marker for `Enter` and `Menu`
- Expanded `tests/test_menu_ui.py` so the rail must stay narrow and anchored near the right edge.
- Follow-up correction requested:
  - map the five labels to the hardware control locations instead of treating them as a generic vertical list
- Refined the right-edge legend again in `src/menu_ui.py`:
  - `Menu` moved to the top-right hardware button zone
  - `Left`, `Enter`, and `Right` moved into the knob band in top-to-bottom order
  - `Del` moved to the lower back-button zone
  - kept the rail narrow so the legend still reads as edge-aligned hardware guidance
- Updated `tests/test_menu_ui.py` to enforce the hardware-zone ordering.
