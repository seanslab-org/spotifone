# Devlog — 2026-02-22

## Session Start
- Project: Spotifone — Car Thing → Bluetooth mic + keyboard
- Starting from empty project (git + CLAUDE.md + requirements)
- Reference: VibeThing at /Users/seansong/seanslab/VibeThing/

## Progress

### Exploration
- Explored VibeThing reference codebase (370+ lines core, 45+ tests)
- Checked flash prerequisites: libusb installed, pyamlboot missing, firmware exists
- Key VibeThing lessons captured in tasks/lessons.md

### Phase 1 Implementation (complete)
Built all modules with tests in build order:

1. **button.py** (60 lines) + test_button.py (14 tests)
   - ButtonState: press/hold/click state machine
   - ButtonHandler: multi-button manager with callbacks

2. **hid.py** (85 lines) + test_hid.py (20 tests)
   - KeyCodes, HIDKeyMapper (platform → key mapping)
   - HIDService: key state + injected transport client

3. **audio.py** (55 lines) + test_audio.py (14 tests)
   - AudioService: mute/unmute with running state
   - PTTController: bridges button events to audio

4. **service.py** (60 lines) + test_service.py (10 tests)
   - SpotifoneService: single orchestrator wiring PTT → HID + audio

5. **hardware.py** (130 lines) + test_hardware.py (28 tests)
   - InputEventReader: /dev/input/event* parsing
   - BlueZHIDClient: HID report builder
   - ALSAMic: pactl mute/unmute

6. **main.py** (80 lines) — CLI entry point with arg parsing

7. **C daemon stubs** — gattd.c, mic_bridge.c, protocol.h, Makefile

8. **Deployment** — deploy.sh, spotifone.service

### Test Results
- **86 tests, all passing** (0.64s)
- Clean module imports: tests import from src/, not from other test files
- All hardware access mockable via dependency injection

## Architecture Improvements over VibeThing
- Single orchestrator (service.py) vs VibeThing's 3-file split
- Shared IPC protocol header (protocol.h) for C daemons
- Makefile for C builds
- Cleaner test structure (no cross-test imports)

## Blockers
- pyamlboot not installed — needed for Phase 0 device flashing
- C daemons are stubs — need BlueZ D-Bus and ALSA integration

## Next
- Install pyamlboot and flash device
- On-device testing of button input
- Implement full C daemon logic
