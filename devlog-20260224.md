# Devlog — 2026-02-24

## Fix: HID keyboard not working when audio (mic_bridge) is also running

### Problem
Audio (mic_bridge) and HID keyboard (hid_keyboard) each work individually but fail when combined. When both run together, only audio works — keyboard keys never arrive at the Mac.

### Root Causes

**Bug 1: Missing `-P input` in combined mode (`bt_init.sh:87`)**
When `KEYBOARD_FIRST=0` (normal combined mode), bluetoothd started without `-P input`. BlueZ's built-in input plugin bound L2CAP PSM 17/19 internally, preventing hid_keyboard from registering its HID profile. The flag was only present in `KEYBOARD_FIRST=1` mode — a copy-paste omission.

**Bug 2: Dual-agent conflict**
Both daemons registered their own D-Bus pairing agent and called `RequestDefaultAgent`. mic_bridge starts first and wins the slot. When hid_keyboard starts and calls `RequestDefaultAgent`, one of them loses pairing ability.

### Fixes

1. **`scripts/bt_init.sh`**: Added `-P input` to combined-mode bluetoothd startup (line 87). Updated log message.

2. **`daemon/hid_keyboard.c`**: Removed all agent registration code (~230 lines removed):
   - Removed `AGENT_PATH`, `AGENT_IFACE` constants
   - Removed `AGENT_INTROSPECT_XML`
   - Removed `agent_message_handler()` function
   - Removed `register_agent()` function
   - Removed agent vtable, object path registration, and cleanup in `main()`
   - mic_bridge's agent handles all pairing for both profiles

### Verification
- Code reviewed for correctness (no orphaned references)
- File reduced from 881 → 652 lines
- Needs on-device test: deploy, reboot, re-pair, verify both audio + keyboard work

### Lessons
- #39: `-P input` needed in ALL modes, not just keyboard-first
- #40: Only one daemon should register a BlueZ pairing agent

---

## Display: Vertical Boot Screen + Amlogic Boot Logo

### Goal
Replace the stock Spotify "blue house" boot logo and blank/noisy framebuffer with a branded Spotifone vertical boot screen — consistent from U-Boot through runtime.

### Architecture

Two display stages on Car Thing:

1. **Boot logo** (U-Boot) — Amlogic reads `bootup.bmp` from the logo partition (`/dev/logo`), renders to display before kernel starts. Format: 16-bit R5G6B5 BMP packed via `aml_imgpack`.
2. **Runtime framebuffer** (after bt_init.sh) — `setup_display.sh` writes raw BGR888 to `/dev/fb0`. Runs at Step 1b of bt_init (right after GPIO reset, before BT stack).

Both stages show the same visual: black background, mic icon centered on upper portion, "Spotifone" title (64pt white), "Music · Voice · Connected" tagline (28pt gray).

### Implementation

**`scripts/convert_logo.py`** — Single script generates both outputs from `logo.jpeg`:
- `logo.fb` — 480×800 BGR888 (1,152,000 bytes) for `/dev/fb0`
- `bootup.bmp` — 480×800 16-bit R5G6B5 BMP (768,066 bytes) for logo partition
- `logo.png` — preview PNG for visual verification on Mac
- Process: crops mic icon from left 33% of horizontal logo, removes white background via per-channel threshold, scales to 220px wide, composites onto black canvas, renders text with system fonts

**`scripts/setup_display.sh`** — Runtime display init:
- Unbinds ALL vtconsoles (`vtcon0`, `vtcon1`, etc.) to stop fbcon redraws
- Clears framebuffer to black with `dd if=/dev/zero`
- Writes `logo.fb` to `/dev/fb0`
- Called from `bt_init.sh` Step 1b

**`scripts/deploy.sh`** — Updated to push `logo.fb` + `setup_display.sh` to device

**Boot logo flash** — Manual process (one-time per device):
1. `python3 scripts/convert_logo.py` → generates `bootup.bmp`
2. Pull `/dev/logo` dump: `adb shell dd if=/dev/logo of=/tmp/logo.dump`
3. Unpack with `aml_imgpack.py --unpack`
4. Replace `bootup.bmp`
5. Repack with `aml_imgpack.py --pack`
6. Flash: `adb shell dd if=/tmp/logo_new.dump of=/dev/logo`

### Bug Fix: Noisy Framebuffer Background

**Problem**: Runtime framebuffer showed colored noise pixels in black areas.

**Root cause**: `setup_display.sh` tried to unbind `vtcon1` which doesn't exist on Car Thing — only `vtcon0` exists. fbcon stayed active and redrew garbage over the framebuffer after `logo.fb` was written.

**Fix**: Changed to unbind ALL vtconsoles via glob (`/sys/class/vtconsole/vtcon*/bind`), plus pre-clear framebuffer with `dd if=/dev/zero` as safety belt.

### Files Changed
| File | Change |
|------|--------|
| `scripts/convert_logo.py` | Added `save_r5g6b5_bmp()`, refactored to `render_canvas()` + dual output |
| `scripts/setup_display.sh` | NEW — fbcon unbind + framebuffer write |
| `scripts/bt_init.sh` | Added Step 1b: call setup_display.sh |
| `scripts/deploy.sh` | Added logo.fb + setup_display.sh deployment |
| `.gitignore` | Added `logo.fb`, `bootup.bmp` |
| `logo.jpeg` | NEW — source horizontal logo |
| `logo.png` | NEW — generated preview |

### Verification
- `convert_logo.py` generates correct output sizes (logo.fb=1,152,000, bootup.bmp=768,066)
- `setup_display.sh` runs on device, logo renders with clean black background
- Boot logo flashed to `/dev/logo`, verified repack with `aml_imgpack`
- Awaiting reboot test to confirm boot logo renders during U-Boot stage

### Framebuffer Details
- Device: Car Thing (Amlogic S905D2)
- Display: 480×800, portrait
- Framebuffer: `/dev/fb0`, 24bpp BGR888, stride 1440, double-buffered (480×1600 virtual)
- Logo partition: `/dev/logo` (8MB, mmcblk0p7), Amlogic `AML_RES!` packed format
- Boot BMP requirements: 16-bit R5G6B5, BMP with BI_BITFIELDS masks

### Lessons
- #41: Car Thing vtconsole is `vtcon0`, not `vtcon1` — always glob all vtconsoles
- #42: Amlogic boot logos must be 16-bit R5G6B5 BMP — see `/repo/files/logo/Readme.md`

---

## BT Auto-Reconnect on Boot

### Problem
After device reboot, Spotifone becomes discoverable but never actively connects to previously paired hosts. Both HID keyboard and HFP audio profiles are passive — they only accept incoming connections. The user must manually open Mac Bluetooth settings and click "Connect".

### Root Cause
No code anywhere calls `Device1.Connect()` for known paired devices after the BT stack is up. `AutoConnect=true` in RegisterProfile only tells BlueZ to accept incoming reconnections, not to initiate outbound ones.

### Implementation

**`scripts/auto_reconnect.sh`** (NEW) — Standalone reconnect script launched from bt_init.sh:
1. Waits up to 30s for mic_bridge + hid_keyboard PIDs to appear
2. Reads adapter BD address from `hciconfig hci0`
3. Enumerates paired devices from `/var/lib/bluetooth/<adapter>/*/info` (devices with `[LinkKey]`)
4. For each paired device, calls `dbus-send ... Device1.Connect` (all profiles at once)
5. Retries up to 5 times with 10s backoff per device
6. Logs all attempts to `/tmp/auto_reconnect.log`
7. Exits after success or max retries (not a persistent daemon)

**`scripts/bt_init.sh`** — Added Step 11 after the trust loop (Step 10): launches `auto_reconnect.sh &` when not in KEYBOARD_FIRST mode.

**`daemon/hid_keyboard.c`** — Added `AutoConnect=true` to RegisterProfile options dict for consistency with mic_bridge (accepts incoming reconnections).

**`scripts/deploy.sh`** — Added push for `auto_reconnect.sh` to device.

### Design Decisions
- **`Device1.Connect` over `ConnectProfile`**: BlueZ connects all registered profiles (HFP + HID) in one call. Simpler, fewer failure modes.
- **Separate script, not inline in bt_init.sh**: bt_init.sh ends with `wait $BTATTACH_PID` (blocking). Retry loops there would complicate flow. Separate script is independently testable/deployable.
- **10s retry interval**: Host may not be awake at device boot time. 5 attempts × 10s = up to ~50s total wait.

### Files Changed
| File | Change |
|------|--------|
| `scripts/auto_reconnect.sh` | NEW — reconnect loop script |
| `scripts/bt_init.sh` | Step 11: launch auto_reconnect.sh in background |
| `daemon/hid_keyboard.c` | `AutoConnect=true` in RegisterProfile options |
| `scripts/deploy.sh` | Push auto_reconnect.sh to device |
| `tasks/todo.md` | Auto-reconnect bug marked resolved |
| `tasks/lessons.md` | Lesson #43: AutoConnect vs Device1.Connect |

### Verification
- Awaiting on-device deploy + reboot test
- Expected: Mac shows "Connected" within ~30s of device boot without manual intervention
- Check `/tmp/auto_reconnect.log` for reconnect attempts and outcomes

### Lessons
- #43: `AutoConnect=true` in RegisterProfile only accepts incoming; explicit `Device1.Connect` needed for device-initiated reconnection

---

## Phase 2 Menu UI (Foundation)

### Requirement
Add an on-device menu UI with:
- Bluetooth Host List (reconnect + delete)
- Scan for Host
- Version/About

Constraint from user: keep all existing button/HID behavior unchanged; only add mute button (`KEY_M` / code 50) as menu toggle. Menu interaction is touch-only.

### Implementation
- `docs/phase2-menu-sdd.md`: SDD covering states, input scheme, BlueZ D-Bus calls, failure modes, and verification checklist
- `src/menu_ui.py`: touch + framebuffer menu UI (renders to `/dev/fb0`, reads `/dev/input/event3`)
- `daemon/button_listener.c`: mute button press sends `0x01` toggle to `/tmp/spotifone_menu.sock`
- `scripts/bt_init.sh`: starts `menu_ui.py` via `start-stop-daemon --exec /usr/bin/python3`
- `scripts/deploy.sh`: pushes `/opt/spotifone/VERSION` (git describe/sha) for UI display

### Verification
- Menu daemon runs on device and binds `/tmp/spotifone_menu.sock`
- IPC toggle changes framebuffer contents (hash changes) and logs toggle transitions to `/tmp/menu.log`
- Remaining manual validation pending: touch connect/delete flows + scan results usability + confirm no key regressions during real usage

---

## UI Refresh: walkie.sh-inspired text-only screens (purple theme)

### Request
Adopt the visual style of https://walkie.sh/:
- Text-first, minimal dark UI
- Structured palette (bg/surface/border/text/muted)
- Keep Spotifone purple as the accent color
- Discard bitmap icon art for boot + runtime background; use styled text only

### Fix: "static/noise" pixels on framebuffer
**Root cause**: Car Thing fb0 is double-buffered (`virtual_size=480,1600` for a `480x800` panel). Writing only the first page leaves the second page uninitialized; any pan/flip shows colored garbage.

**Fix implemented**:
- `scripts/setup_display.sh`: clear 1600 rows and mirror `logo.fb` into both pages
- `src/menu_ui.py`: `Framebuffer.present()` mirrors the rendered buffer into all virtual pages

### Text-only branding assets
Updated `scripts/convert_logo.py` to generate a walkie-style, text-only splash:
- Title with purple accent on `ONE`
- Muted 2-line tagline
- Monospace "install box" style key-hints block

Generated outputs:
- `logo.fb` (runtime background)
- `bootup.bmp` (U-Boot splash replacement via `/dev/logo`)
- `logo.png` (preview)

### Menu styling
Updated `src/menu_ui.py`:
- Walkie-inspired palette (bg/surface/border/text/muted) with purple accent
- Layout labels: `01 HOSTS`, `02 SCAN`, `03 ABOUT`
- Fixed font fallback bug where spaces rendered as `?`
- Reduced host rows so scan results + about footer do not overlap

### Verification
- Local previews generated: `logo.png`, `menu_preview.png`
- Pending on-device verification: deploy + reboot; confirm no background noise and menu toggle works
